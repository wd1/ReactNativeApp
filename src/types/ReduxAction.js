// @flow

/**
 * A redux action that gets dispatched
 *
 * @typedef {Object} ReduxAction
 * @property {String} type The redux action type
 * @property {Any} payload The redux action payload, could be anything
 */
